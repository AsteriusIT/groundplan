// External product URLs. The app URL is the staging host for now — swap here
// once the real domain lands (single source, used by the header and the CTA).
export const APP_URL = "https://app.groundplan.qcs.ovh/";
