/**
 * Drizzle schema entrypoint.
 *
 * No tables yet — this bootstrap ticket (GP-2) only wires up the database
 * connection and migration tooling. Real tables arrive with later stories.
 */

export {};
