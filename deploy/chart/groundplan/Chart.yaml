apiVersion: v2
name: groundplan
description: >-
  Groundplan — living, reviewable diagrams of your Terraform and Kubernetes
  estate. Deploys the API, the frontend and an ingress; database and identity
  provider are external by default, with embedded evaluation-only options.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Default image tag ("latest" = the most recent tagged release of the app
# images on ghcr.io/asteriusit). Pin with image.tag in values for production.
appVersion: latest
