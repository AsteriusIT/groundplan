{{/*
Chart-managed secrets — rendered only for the inline-value alternatives.
Production brings its own Secrets and references them by name (existingSecret),
in which case nothing is emitted here.
*/}}
{{- if and (not .Values.api.existingSecret) .Values.api.encryptionKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "groundplan.fullname" . }}-api
  labels:
    {{- include "groundplan.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
type: Opaque
stringData:
  ENCRYPTION_KEY: {{ .Values.api.encryptionKey | quote }}
{{- end }}
{{- if .Values.postgresql.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "groundplan.fullname" . }}-db
  labels:
    {{- include "groundplan.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
type: Opaque
stringData:
  password: {{ .Values.postgresql.password | quote }}
{{- else if and (not .Values.externalDatabase.existingSecret) .Values.externalDatabase.password }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "groundplan.fullname" . }}-db
  labels:
    {{- include "groundplan.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
type: Opaque
stringData:
  {{ .Values.externalDatabase.passwordKey }}: {{ .Values.externalDatabase.password | quote }}
{{- end }}
{{- if and .Values.ai.apiKey (not .Values.ai.existingSecret) (not (include "groundplan.aiExternalSecretActive" .)) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "groundplan.fullname" . }}-ai
  labels:
    {{- include "groundplan.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
type: Opaque
stringData:
  AI_API_KEY: {{ .Values.ai.apiKey | quote }}
{{- end }}
